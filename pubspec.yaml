name: jump_worlds
description: "A jumping game with generated worlds, enemies, and AI voices."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.8.0-278.2.beta

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.28.1
  flame_audio: ^2.11.5
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flame_lint: ^1.3.0

flutter:
  uses-material-design: true
